use bevy::prelude::*;

/// TODO: Do we really need this?
#[derive(Component, Default, Debug)]
pub struct Grid;

/// TODO: Do we really need this?
#[derive(Component, Default, Debug)]
pub struct GridCell;
